# Setup — 30 seconds

```bash
npm install
npm install -g .
aiops
```

Done.

## That's it?

Yes. The binary is pre-built and in git. No build, no esbuild, no platform issues.

## For teams

Send them the repo, they run those 3 commands.
